# Releases

Suggested GitHub Release assets:

- `best.pt`
- `best.onnx`
- Checksum file, if possible
- Demo video, only if owned by the maintainer or clearly licensed

For this initial release, the model files are small enough to be committed directly in `models/`.
